// WAP to accept n int from user and store them in an array. Sort the arrray in ascending order using 
// bubble sort.
// Then accept another number from the user search whether that number is exists in an array using binary
// search.
// If it does display its index and if it does not then print that "The number is not found, in the 
// array."
#include<iostream>
using namespace std;

class Binary{
    int beg;
    int end;
    int mid;
    int arr[];
public:
    Binary(){
        beg=0;
        end=0;
        mid=0;
    }
    void getData(){
        
    }
};